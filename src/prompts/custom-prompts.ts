import { IPromptData } from 'fa-mcp-sdk';

/**
 * Additional MCP prompts beyond AGENT_BRIEF / AGENT_PROMPT.
 * None are defined for this server — the agent brief and prompt are sufficient.
 */
export const customPrompts: IPromptData[] = [];
