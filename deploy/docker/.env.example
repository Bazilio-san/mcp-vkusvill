# ── Image tag (set by CI deploy job) ───────────────────────
IMAGE_TAG=mcp-vkusvill:latest

# ── Host port (external port on the server) ────────────────
HOST_PORT=9048

# ── Service identity ──────────────────────────────────────
SERVICE_NAME=mcp-vkusvill
SERVICE_INSTANCE=prod
NODE_ENV=production
